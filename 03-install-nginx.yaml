- name: install NGINX
  hosts: local
  become: yes
  tasks:
  - name: Installing Nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Started the Nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
